{
  "name": "nori-docs",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "npm run generate && astro build",
    "preview": "astro preview",
    "generate": "npm run generate:errors && npm run generate:api",
    "generate:errors": "node generators/generate-error-docs.mjs",
    "generate:api": "node generators/generate-api-docs.mjs"
  },
  "dependencies": {
    "astro": "^5.3.0",
    "@astrojs/starlight": "^0.32.0",
    "sharp": "^0.33.0"
  }
}
